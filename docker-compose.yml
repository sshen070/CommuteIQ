version: "3.9"

services:
  postgres:
    image: postgres:18

    environment:
      POSTGRES_DB: commute_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password

    ports:
      - "5432:5432"